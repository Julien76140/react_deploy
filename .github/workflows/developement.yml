name: React julien development

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Install SSH KEY
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          know_hosts: ${{ secrets.SSH_HOST }}
      - name: PULL project
        run: ssh julien@51.178.30.100 "cd /home/julien/web/ && /usr/bin/git pull origin master"